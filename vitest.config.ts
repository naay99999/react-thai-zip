import { defineConfig } from 'vitest/config'

// Project definitions, per-library `@/components/ui/*` alias maps, and test
// globs now live in vitest.workspace.ts (Vitest 2's workspace mechanism) —
// this file only holds settings vitest still reads at the workspace root.
export default defineConfig({})
